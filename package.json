{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "cryptoman",
  "scripts": {
    "dev": "pnpm --filter extension run dev",
    "test": "pnpm --recursive --filter !@cryptoman/connector run test:unit",
    "test:chain": "pnpm --recursive --filter @cryptoman/chain run test:unit",
    "test:eth": "pnpm --recursive --filter @cryptoman/eth run test:unit"
  },
  "devDependencies": {
    "@biomejs/biome": "2.1.1",
    "@total-typescript/ts-reset": "0.6.1",
    "@total-typescript/tsconfig": "1.0.4",
    "typescript": "5.8.3",
    "vite": "7.0.5",
    "vite-tsconfig-paths": "5.1.4",
    "vitest": "^3.2.4"
  }
}

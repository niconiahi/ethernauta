// Unicode UCD version that adraffy's bundled NFC tables are
// derived from. Kept as a re-exportable constant so callers can
// surface the bundled Unicode version. Update alongside any
// `@adraffy/ens-normalize` version bump.

export const UCD_VERSION = "16.0.0" as const

// https://eips.ethereum.org/EIPS/eip-137
// https://docs.ens.domains/ensip/15

// Phase 1a — data derivation infrastructure only.
// Phase 1b will add `nfc` / `nfd`.
// Phase 1c will add `ens_normalize`.

export {
  CCC_DATA,
  COMP_DATA,
  DECOMP_DATA,
  UCD_VERSION,
} from "./data/nfc"

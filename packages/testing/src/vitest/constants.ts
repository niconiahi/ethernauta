// Shared constants for the plugin <-> setup-file communication
// channel. Lives in its own module so both sides import the
// same literal without a cross-import.

export const OPTIONS_ENV_VAR = "ETHERNAUTA_ANVIL_OPTIONS"

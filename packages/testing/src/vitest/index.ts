// `/vitest` subpath for `@ethernauta/testing`. The
// `ethernautaAnvil()` plugin lands here in Phase 5; for now this
// file only carries the placeholder so the tsdown build emits the
// entrypoint.

export {}
